language: cpp

before_install:
  - chmod +x do-configure

before_script:
  - git clone https://github.com/duongdoOrg/Trilinos
  - mkdir BUILD
  - cd BUILD
  
script:
  - ./do-configure -DTrilinos_ENABLE_Teuchos=ON
